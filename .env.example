# =============================================================================
# RAG GPU Server - Environment Variables
# =============================================================================

# Server
HOST=0.0.0.0
PORT=8000

# Models
EMBEDDING_MODEL=BAAI/bge-m3
RERANKER_MODEL=BAAI/bge-reranker-v2-m3

# vLLM (container separado)
VLLM_BASE_URL=http://localhost:8080/v1
VLLM_MODEL=Qwen/Qwen3-8B-AWQ

# Hardware
DEVICE=cuda
USE_FP16=true

# Hugging Face cache
HF_HOME=/root/.cache/huggingface

# Git (para startup script)
REPO_URL=https://github.com/seu-usuario/rag-gpu-server.git

# =============================================================================
# Artifacts Uploader (PR13/Etapa 4)
# =============================================================================
# URL base da VPS para upload de artifacts
ARTIFACTS_BASE_URL=https://vectorgov.io

# Endpoint de upload (default: /api/v1/ingest/artifacts)
# ARTIFACTS_INGEST_ENDPOINT=/api/v1/ingest/artifacts

# API Key para autenticação na VPS
VPS_API_KEY=vg_xxx_your_api_key

# Cloudflare Access (opcional, se protegido por Access)
# CF_ACCESS_CLIENT_ID=your_client_id
# CF_ACCESS_CLIENT_SECRET=your_client_secret
